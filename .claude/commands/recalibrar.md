---
allowed-tools: Bash, Read, Write, Edit, Glob, Grep, AskUserQuestion
description: Reajusta el plan con el ritmo real (planificación inversa, sin promesas falsas)
argument-hint: [asignatura]
---

# Gatillo: recalibrar $ARGUMENTS

1. Lee y obedece `methodology/00-MANIFEST.md` (protocolo de arranque: carga `10` + `perfil/PSICOLOGIA.md`).
2. Ejecuta la **§5 (Recalibración) de `methodology/04-PLANIFICACION.md`** para la asignatura
   `$ARGUMENTS`, usando las métricas de los logs (`06`).

Di la verdad sobre si la nota objetivo es alcanzable al ritmo real; re-prioriza al subconjunto de
mayor rendimiento; reescribe `PLAN.md` y actualiza `PANORAMA.md`. Si la caída huele a evitación,
trátalo también desde `10`. Hablas en español.
